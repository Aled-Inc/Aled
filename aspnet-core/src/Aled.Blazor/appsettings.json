{
  "App": {
    "SelfUrl": "{{App.SelfUrl}}"
  },
  "RemoteServices": {
    "Default": {
      "BaseUrl": "{{Remote.Default.BaseUrl}}"
    }
  },
  "Redis": {
    "Configuration": "{{Redis.Configuration}}"
  },
  "AuthServer": {
    "Authority": "{{AuthServer.Authority}}",
    "RequireHttpsMetadata": true,
    "ClientId": "{{AuthServer.ClientId}}",
    "ClientSecret": "{{AuthServer.ClientSecret}}",
    "IsContainerized": false
  },
  "StringEncryption": {
    "DefaultPassPhrase": "{{StringEncryption.DefaultPassPhrase}}"
  }
}
